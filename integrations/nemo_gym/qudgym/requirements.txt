# Install the local QudGym checkout into this resource server's environment.
# No PyPI package named "qudgym" is assumed to have been published.
# Example: python -m pip install /absolute/path/to/QudGym
